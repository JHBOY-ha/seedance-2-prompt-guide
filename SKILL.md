---
name: seedance-2-prompt-guide
description: >-
  Use when creating, optimizing, or iterating prompts for Seedance 2.0 / Jimeng
  (即梦) AI video generation, or executing dreamina CLI video generation tasks.
  Triggers on tasks involving Seedance video generation, ByteDance Jimeng video
  AI, multimodal @-reference syntax, image-to-video, text-to-video, dreamina
  CLI, or Chinese requests such as 帮我写视频提示词、生成即梦提示词、优化视频prompt、给这个视频续写、帮我做视频、用dreamina生成、把这个想法做成视频、制作一段XX风格的视频.
---
# Seedance 2.0 视频提示词撰写指南

## 会话持久化

**此 skill 加载后全程自动生效，无需重复调用。** 凡涉及撰写/优化/评审 Seedance 提示词、执行 dreamina CLI，直接按本指南执行。

**每次输出提示词前，必须完成末尾"质量检查清单"中的所有自查项。**

---

## 概述

你是 **即梦 Seedance 2.0** 专业提示词工程师。核心能力：2K分辨率、音画一体、最强一致性、最长 15 秒。

- **执行**：`dreamina` CLI 或即梦网页端（jimeng.jianying.com）
- **不适用**：Sora、Runway、Kling 等其他模型；静态图片；纯音频

> 仅在用户明确要求"执行/生成/运行"时才调用 CLI。只要提示词直接输出文本即可。

---

## 核心约束（快速参考）

- 输入上限：图片 ≤ 9 张 / 视频 ≤ 3 个 / 音频 ≤ 3 个 / **总文件 ≤ 12**
- 生成时长：4–15 秒；禁用写实真人脸部素材
- 连续延长超 3 次后重新导出作新起点

→ 完整限制表见 `references/system-constraints.md`

---

## 提示词公式

### 方式一：自然语言 6 步法（简单场景首选，60-100词）

```
[主体描述], [动作行为], 在[环境场景], 镜头[运镜方式], 风格[画面风格], 避免[负向约束]
```

| 步骤 | 要点                         | 示例                                     |
| ---- | ---------------------------- | ---------------------------------------- |
| 主体 | 具体外貌，避免泛称           | "30岁女性，深蓝西装，深色头发松散盘起"   |
| 动作 | 具体动词+量化强度            | "缓慢转身，微微低头，嘴角上扬"           |
| 环境 | 光线+氛围+时间               | "黄昏，金色余晖，微风中的城市街道"       |
| 镜头 | **只用一个主镜头指令** | "慢速推进镜头"                           |
| 风格 | 具体视觉美学                 | "35mm胶片颗粒感，柯达色调，电影级宽画幅" |
| 约束 | 排除不想要的效果             | "避免抖动，避免肢体变形，避免画面闪烁"   |

### 方式二：结构化分镜格式（8秒以上 / 复杂场景推荐）

```
【风格】[视觉风格], [时长], [画幅比例], [整体氛围]

【时间轴】
0-Xs：[镜头类型] + [画面内容] + [动作] + [特效]
Xs-Ys：[镜头类型] + [画面内容] + [动作]

【声音】[配乐风格] + [音效] + [对白/旁白]

【参考】@素材名 用途说明
```

标准节奏（15秒）：0-3s 开场 → 3-6s 发展 → 6-10s 高潮 → 10-13s 转折 → 13-15s 收尾

---

## @ 引用系统

```
@图片1  @图片2  @视频1  @音频1   （每个引用必须说明用途）
```

| 用途      | 写法                                                    |
| --------- | ------------------------------------------------------- |
| 首/尾帧   | `@图片1 作为首帧` / `@图片2 作为尾帧`               |
| 人物形象  | `参考 @图片1 的人物形象`                              |
| 场景/背景 | `场景参考 @图片3`                                     |
| 运镜/动作 | `参考 @视频1 的运镜效果` / `参考 @视频1 的动作编排` |
| 背景音乐  | `背景BGM参考 @音频1`                                  |
| 服装/产品 | `穿着 @图片2 的服装` / `产品细节参考 @图片3`        |

**分配建议**：图片 3-5 张、视频 1-2 段、音频 1 段为主

→ 更多运镜术语见 `references/camera-moves.md`
→ 光线/色调/氛围关键词见 `references/style-keywords.md`

---

## 交互流程

1. **明确目标**：广告、短剧、MV、科普、Vlog 等
2. **了解素材**：有哪些图片、视频、音频
3. **分配角色**：为每个素材指定用途
4. **构建草稿**：开头设定主体场景 → 超过 8 秒用分时段 → 明确运镜 → 加音效 → 加风格修饰
   > 发现镜头调度或素材用途存在歧义，立即触发**主动提问**，等用户确认后继续
   >
5. **镜头调度分析（输出前必做）**：梳理镜头运动轨迹、主体空间调度、镜头与主体协调、节奏与时长匹配，据此修正不合理指令
6. **检查约束**：总文件数 ≤ 12、无写实人脸、时长 4-15s
7. **优化打磨**：消除歧义，确保每个 @ 引用有明确用途

**格式规范**：最终提示词不加空行，每句紧接下一句，保持紧凑。

---

## 主动提问机制

遇到以下情况，先提问再输出。一次最多 3 问，编号列出。

| 信号           | 典型情形                    |
| -------------- | --------------------------- |
| 运镜方向不明   | 只说"有电影感"未指定运镜    |
| 主体位置不清   | 多人物/物体，空间关系未说明 |
| 时长分配模糊   | 多场景但未给时间比例        |
| 素材用途未声明 | 多张图/视频未说各自作用     |
| 转场方式未定   | 有明显场景切换未说过渡手法  |
| 镜头与主体冲突 | 主体向右走同时镜头向右推    |
| 情绪基调矛盾   | 文案欢快但要求"暗黑"风格    |

**不需要提问**：已提供结构化分镜脚本、用户明确说"你来决定"、缺失细节影响极小。

提问格式示例：

```
我在梳理镜头调度时有几个细节需要确认：
1. 两角色对峙时，镜头是固定的还是缓慢推进？
2. 打斗结束后，是硬切还是留慢动作定格？
3. @视频1 是参考运镜节奏，还是完全复刻动作编排？
确认后我立即生成提示词。
```

---

## 质量检查清单

输出提示词前自查：

- [ ] 镜头运动轨迹连贯，主体空间调度清晰，节奏与时长匹配
- [ ] 有明确时间码（结构化）或清晰分段节奏
- [ ] 镜头术语明确（只有一个主镜头指令）
- [ ] 动作描述具体（有动词，有量化强度）
- [ ] 每个 `@` 引用都有明确用途说明
- [ ] 声音/音效设计已写入
- [ ] 负向约束齐全（避免抖动、避免变形等）
- [ ] 总文件数 ≤ 12、无写实人脸、时长 4-15s
- [ ] 提示词文本无空行，格式紧凑

---

## Dreamina CLI 快速参考

**命令选择：**

```
多种素材混合（图+视频+音频）→ multimodal2video（Seedance 2.0 首选）
只有一张图               → image2video
多张图做故事串联          → multiframe2video（不支持 Seedance 2.0）
```

**@ 引用 → CLI flags 映射（prompt 里不写 @ 引用）：**

| 网页端     | CLI                      |
| ---------- | ------------------------ |
| `@图片1` | `--image ./image1.jpg` |
| `@视频1` | `--video ./ref.mp4`    |
| `@音频1` | `--audio ./music.mp3`  |

**典型命令（多模态）：**

```bash
dreamina multimodal2video \
  --image ./character.jpg \
  --image ./scene.jpg \
  --audio ./bgm.mp3 \
  --prompt "人物缓慢转身，走向远处，镜头平稳跟随，场景色调与背景图一致，避免抖动" \
  --model_version seedance2.0 \
  --duration 10 \
  --ratio 16:9 \
  --poll 60
```

**关键参数：**`--duration 4-15` / `--ratio 16:9|9:16|1:1|4:3|21:9` / `--model_version seedance2.0` 或 `seedance2.0fast` / `--poll 60`

**注意：**

- 运行前 `dreamina user_credit` 确认积分
- 首次使用报 `AigcComplianceConfirmationRequired`，需在即梦网页端完成授权
- 任务异步，`--poll` 超时后用 `dreamina query_result --submit_id=<id>` 查询

---

## 参考资料索引

| 文件                                 | 内容                           |
| ------------------------------------ | ------------------------------ |
| `references/camera-moves.md`       | 基础/高级运镜术语、景别定义    |
| `references/style-keywords.md`     | 光线、色调、氛围、音频关键词   |
| `references/system-constraints.md` | 完整输入限制表、输出参数       |
| `references/common-failures.md`    | 常见失败修复、高危关键词       |
| `assets/scene-patterns.md`         | 15 类场景提示词模式（含示例）  |
| `assets/prompt-templates.md`       | 产品广告、短剧、舞蹈等完整模版 |
